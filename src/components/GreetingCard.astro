---
import H1 from "./H1.astro";
import { Image } from "astro:assets";
import { getCollection } from "astro:content";

const homepage = await getCollection("homepage");
---

<section class="flex max-sm:flex-col flex-row gap-x-4 max-sm:justify-normal max-sm:items-center justify-evenly max-sm:gap-y-6">
    <div class="max-w-96 min-w-48 max-sm:max-w-56 flex items-center">
        <div class="w-full">
            <Image src={homepage[0].data.image} alt="Фото репетитора" />
        </div>
    </div>  
    <div class="max-w-[30rem] max-sm:w-fit flex flex-col justify-center space-y-3">
        <div class="max-sm:text-start text-end">
            <H1 text={homepage[0].data.title} />
            <p class="w-full">Lorem ipsum dolor sit amet consectetur, adipisicing elit. Aliquid porro excepturi quos deserunt vel dolore, placeat omnis recusandae nisi quidem iusto rerum ducimus possimus facere laborum quam debitis numquam beatae.</p>
        </div>
            <div class="flex justify-end">
            <button class="rounded px-4 py-3 bg-teal-500 text-white font-semibold text-lg hover:bg-teal-600">Записаться</button>
        </div>
    </div>
</section>