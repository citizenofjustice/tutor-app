---
import H2 from "./anchorH2.astro";
import type { z } from "astro:content";
import { Icon } from 'astro-icon/components';
import type { methodSchema } from "../content/config";


interface Props {
  methods: z.infer<typeof methodSchema>[],
}

const { methods } = Astro.props;
---

<section class="space-y-4" >
    <H2 text="О наших занятиях" id="lessons" ariaLabel="О наших занятиях" />
    <ul class="flex max-lg:flex-col flex-row justify-evenly gap-4">
        {methods.map((method)=>
            <li class="max-lg:text-start max-vsm:text-center text-center max-lg:flex max-vsm:flex-col max-vsm:items-center max-lg:flex-row max-lg:gap-x-5 block max-lg:max-w-[90%] max-vsm:max-w-none even:flex-row-reverse max-vsm:even:flex-col max-lg:even:self-end max-lg:even:text-end max-vsm:even:text-center max-w-64 shadow shadow-primary-200 border-primary-200 rounded-md p-4 max-lg:space-y-0 space-y-3 bg-secondary-50">
                <div class="max-lg:w-12 max-lg:shrink-0 flex justify-center items-center">
                    <Icon name={method.icon} title={method.alt} width={60} height={60} class={`${method.color}`}/>
                </div>
                <div class="max-lg:grow  grid grid-rows-[minmax(0,1fr)] text-text-800 gap-y-1">
                    <h2 class="self-center font-bold">{method.title}</h2>
                    <p class="text-sm">{method.description}</p>
                </div>
            </li>)
        }
    </ul>
</section>