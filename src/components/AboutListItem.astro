---
type Props = {
    key: string,
    value: string,
}

const {key, value} = Astro.props;
---

<li class="grid grid-cols-[200px_minmax(0,_1fr)] max-lg:grid-cols-1 py-3 gap-x-8 items-center">
    <h2 class="font-bold text-lg">{key}</h2>
    <p>{value}</p>
</li>